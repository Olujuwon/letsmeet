export function addTwoNumbers(num1, num2){
    let sum = num1 +  num2;
    return sum; 
}

export const userObject =(name, age, username, password)=>{
    return{
        Name: name,
        Age: age,
        Username: username,
        Password: password
    }
}

//react-scripts test --env=jsdom

